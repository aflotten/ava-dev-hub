<p id="notice"><%= notice %></p>

<div class="container">
  <p>
    <strong>Title:</strong>
    <%= @meeting.title %>
  </p>

  <p>
    <strong>Body:</strong>
    <%= @meeting.body %>
  </p>

  <%= link_to 'Edit', edit_meeting_path(@meeting) %> |
  <%= link_to 'Back', meetings_path %>
</div>
